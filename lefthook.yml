pre-commit:
  commands:
    format-and-lint-swift:
      glob: "*.swift"
      run: make format lint SWIFT_FILES="'{staged_files}'"
      stage_fixed: true
    lint-workflows:
      glob: ".github/{workflows/*.{yml,yaml},actionlint.{yml,yaml}}"
      run: make lint-workflows
